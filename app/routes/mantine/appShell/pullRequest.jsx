import { Title, Space, Text, Group, Button, Notification } from '@mantine/core';
import { showNotification } from '@mantine/notifications';
import { useModals } from '@mantine/modals';

export default function AppShellPullRequest() {    
    return (
      <>
        Pull Requests
      </>
    );
  }
  